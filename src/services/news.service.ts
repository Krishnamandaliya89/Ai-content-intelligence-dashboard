// src/services/news.service.ts
// Optional service layer for abstracting logic away from components/hooks if needed.
// Currently handled heavily by RTK Query.

export const NewsService = {
  // TODO: Add complex business logic for news here if needed
};
