// src/services/transformers.service.ts
// Additional abstraction for transformations if needed

import * as transformers from "@/utils/feed-transformers";

export const TransformerService = {
  ...transformers,
};
